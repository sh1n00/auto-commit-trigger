name: auto-commit

on:
  push:
  pull_request:

jobs:
  formatted:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
          ref: ${{ github.head_ref }}
      - run: |
          pwd
          ls
          ls ~
          touch test
          echo "this is test" >> test
          git branch
          git remote -v
      - uses: stefanzweifel/git-auto-commit-action@v4.15.1
        with:
          commit_message: Generate OpanApi YAML

  build:
    runs-on: ubuntu-18.04
    needs: formatted
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - run: |
          git log --oneline
          git branch
          git fetch
          git log --oneline
          git remote -v

